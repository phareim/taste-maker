// Refine-ritual thresholds. Both are first guesses to revisit once there's
// real usage data — not tuned against anything yet.
export const PROMOTE_THRESHOLD = 3 // wins -> canon
export const ARCHIVE_THRESHOLD = 4 // losses - wins >= this -> archived
